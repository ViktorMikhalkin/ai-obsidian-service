#!/usr/bin/env sh
set -e
#
#if [ "${HUSKY_SKIP_TESTS:-0}" = "1" ]; then
#  echo "husky pre-push: tests skipped by HUSKY_SKIP_TESTS=1"
#  exit 0
#fi
#
#if command -v pytest >/dev/null 2>&1; then
#  echo "husky pre-push: running pytest..."
#  pytest -q || exit 1
#else
#  echo "husky pre-push: pytest not found, skipping"
#fi
